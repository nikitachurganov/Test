# ─── Supabase ────────────────────────────────────────────────────────────────
# Find these in your Supabase project: Settings → API
VITE_SUPABASE_URL=https://koznfzrvjhuisrpehwbd.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_KQqtL_ZC9nbLfkTiS-8YzA_BB9CKNyg

# ─── App URL ─────────────────────────────────────────────────────────────────
# Base URL of the frontend app. Used to build auth redirect URLs
# (email confirmation, password reset). In production set to your domain.
VITE_APP_URL=http://localhost:5173

# ─── SMTP (Supabase Dashboard only — never add secrets here) ─────────────────
# Custom SMTP is configured in the Supabase Dashboard:
#   Project → Authentication → SMTP Settings
#
# For Resend SMTP:
#   Host:     smtp.resend.com
#   Port:     465
#   Username: resend
#   Password: re_YOUR_API_KEY
#   Sender:   noreply@yourdomain.com
#
# These values are stored securely in Supabase and are NOT exposed to the client.
